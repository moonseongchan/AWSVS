import React from "react";

import "./Setting.scss";

const Setting = (props) => {
  return (
    <div
      class="tab-pane fade"
      id="pills-contact"
      role="tabpanel"
      aria-labelledby="pills-contact-tab"
      tabindex="0"
    >
      Setting
    </div>
  );
};

export default Setting;
